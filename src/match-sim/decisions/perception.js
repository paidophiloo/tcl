import { inspectPassingLane } from '../spatial/passing-lanes.js'; import { pressureAtPoint, nearestPressure } from '../spatial/pressure-map.js'; import { controlForTeam } from '../spatial/pitch-control.js'; import { offsideLineForAttack } from '../spatial/offside-line.js'; import { localPoint, normalizedDistance, zoneOf } from '../spatial/pitch-model.js'; import { phaseFor } from '../tactics/tactical-shapes.js';
export function perceive(state,teamIndex,player){const team=state.teams[teamIndex],opp=state.teams[1-teamIndex],local=localPoint(team,player),pressure=pressureAtPoint(opp,player),offsideLine=offsideLineForAttack(team,opp,state.ball);const mates=team.players.filter(p=>!p.redCard&&String(p.id)!==String(player.id)).map(p=>{const lp=localPoint(team,p);const lane=inspectPassingLane(player,p,opp);return {player:p,local:lp,distance:normalizedDistance(player,p),progress:lp.x-local.x,lane,control:controlForTeam(state,teamIndex,p),offside:lp.x>offsideLine&&lp.x>.5}});return {teamIndex,player,team,opponent:opp,phase:phaseFor(state,teamIndex),local,zone:zoneOf(team,player),pressure,nearestDefenders:nearestPressure(opp,player,3),offsideLine,mates,scoreDelta:state.score[teamIndex]-state.score[1-teamIndex],minute:state.clockSeconds/60,ball:state.ball}}
