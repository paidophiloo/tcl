import { contextualSkill } from '../core/player-attributes.js'; import { perceive } from './perception.js'; import { generateOptions } from './option-generation.js'; import { rankOptions } from './option-evaluation.js';
export function decide(state,teamIndex,player,rng){const perception=perceive(state,teamIndex,player);const ranked=rankOptions(perception,generateOptions(perception));const decisions=contextualSkill(player,['decisions','composure','concentration']);const temp=.12+(100-decisions)/330;const choice=rng.softmax(ranked,o=>o.utility,temp)||ranked[0]||{type:'carry',utility:0};return {choice,ranked,perception,evidence:{pressure:+perception.pressure.toFixed(3),phase:perception.phase,zone:perception.zone,offsideLine:+perception.offsideLine.toFixed(3),topOptions:ranked.slice(0,3).map(o=>({type:o.type,targetId:o.target?.id||null,utility:+o.utility.toFixed(3)}))}}}
