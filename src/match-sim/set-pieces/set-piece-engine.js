import { contextualSkill } from '../core/player-attributes.js'; import { aerialDuel } from '../actions/duels.js';
export function chooseSetPieceTaker(team,type='corner'){const keys=type==='penalty'?['penalties','finishing','composure']:type==='freeKick'?['setPieces','technique','longShots']:['crossing','setPieces','technique'];return team.players.filter(p=>!p.redCard&&!p.injured).sort((a,b)=>contextualSkill(b,keys)-contextualSkill(a,keys))[0]||null}
export function resolveCorner(state,teamIndex,rng){const team=state.teams[teamIndex],opp=state.teams[1-teamIndex],taker=chooseSetPieceTaker(team,'corner'),attackers=team.players.filter(p=>!p.redCard&&p.id!==taker?.id).sort((a,b)=>contextualSkill(b,['aerial','jumping','strength'])-contextualSkill(a,['aerial','jumping','strength'])),defenders=opp.players.filter(p=>!p.redCard&&p.role!=='GK').sort((a,b)=>contextualSkill(b,['aerial','jumping','positioning'])-contextualSkill(a,['aerial','jumping','positioning']));const a=attackers[0],d=defenders[0];if(!a||!d)return {won:false};const duel=aerialDuel(a,d,rng);return {won:duel.firstWins,attacker:a,defender:d,taker,duel,xG:duel.firstWins?.08:0}}
