import { contextualSkill } from '../core/player-attributes.js';
export function injuryRisk(player,{contact=false,activity='jog'}={}){const energy=Math.max(0,100-(player.stamina??90))/100,fitness=contextualSkill(player,['stamina','physical','balance'],{fatigueWeight:0})/100,load=Math.min(2,(player.matchLoad||0)/25),activityRisk=['sprint','duel','jump','dribble'].includes(activity)?1.35:1;return Math.max(.000001,(.000004+energy*.000018+load*.000006)*(1.18-fitness*.3)*activityRisk*(contact?2.2:1))}
export function maybeInjure(player,rng,context){if(player.injured)return null;const risk=injuryRisk(player,context);if(!rng.chance(risk))return null;const severity=rng.next();const injury={severity:severity>.96?'serious':severity>.72?'moderate':'minor',risk};player.injured=injury;return injury}
