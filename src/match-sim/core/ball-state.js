import { clamp01 } from '../spatial/pitch-model.js';
export function createBallState(){return {x:.5,y:.5,z:0,vx:0,vy:0,vz:0,spin:0,carrierId:null,lastTouchPlayerId:null,lastTouchTeamIndex:null,state:'dead',action:null,intendedTargetId:null}}
export function attachBall(ball,player,teamIndex=null){ball.carrierId=player?.id??null;ball.lastTouchPlayerId=player?.id??ball.lastTouchPlayerId;if(teamIndex!=null)ball.lastTouchTeamIndex=teamIndex;if(player){ball.x=player.x;ball.y=player.y;ball.z=0}ball.vx=ball.vy=ball.vz=0;ball.state=player?'carried':'loose';ball.action=null;ball.intendedTargetId=null;return ball}
export function launchBall(ball,{from,to,duration=.5,height=0,type='pass',targetId=null,teamIndex=null,playerId=null,outcome=null}){ball.x=from.x;ball.y=from.y;ball.z=from.z||0;ball.carrierId=null;ball.lastTouchPlayerId=playerId??ball.lastTouchPlayerId;if(teamIndex!=null)ball.lastTouchTeamIndex=teamIndex;ball.intendedTargetId=targetId;ball.state='flight';ball.action={type,from:{x:from.x,y:from.y,z:from.z||0},to:{x:clamp01(to.x),y:clamp01(to.y),z:to.z||0},duration:Math.max(.08,duration),elapsed:0,height:Math.max(0,height),outcome};return ball}
export function updateBallFlight(ball,dt){if(ball.state!=='flight'||!ball.action)return {arrived:false};const a=ball.action;a.elapsed=Math.min(a.duration,a.elapsed+dt);const t=Math.min(1,a.elapsed/a.duration);const eased=t<.5?2*t*t:1-Math.pow(-2*t+2,2)/2;ball.x=a.from.x+(a.to.x-a.from.x)*eased;ball.y=a.from.y+(a.to.y-a.from.y)*eased;ball.z=(a.from.z+(a.to.z-a.from.z)*t)+Math.sin(Math.PI*t)*a.height;if(t>=1){ball.x=a.to.x;ball.y=a.to.y;ball.z=a.to.z||0;ball.state='arrived';return {arrived:true,action:a}}return {arrived:false,action:a}}
