import { contextualSkill } from '../core/player-attributes.js'; import { goalDistance,goalAngleQuality,localPoint } from '../spatial/pitch-model.js'; import { pressureAtPoint } from '../spatial/pressure-map.js';
const clamp=(v,a=0,b=1)=>Math.max(a,Math.min(b,v));
export function shotQuality(state,teamIndex,shooter){const team=state.teams[teamIndex],opp=state.teams[1-teamIndex],d=goalDistance(team,shooter),angle=goalAngleQuality(team,shooter),pressure=pressureAtPoint(opp,shooter),fin=contextualSkill(shooter,['finishing','composure','technique','decisions'])/100;const loc=localPoint(team,shooter);const spatial=.015+Math.pow(clamp(1-d),2.55)*.36;const xg=clamp(spatial*angle*(.78+fin*.3)*clamp(1-pressure*.16,.5,1),.006,.55);return {xg,pressure,distance:d,angle,local:loc}}
export function planShot(state,teamIndex,shooter,rng){const q=shotQuality(state,teamIndex,shooter),team=state.teams[teamIndex],opp=state.teams[1-teamIndex],gk=opp.players.find(p=>p.role==='GK'&&!p.redCard)||opp.players.find(p=>!p.redCard),fin=contextualSkill(shooter,['finishing','composure','technique']),keep=gk?contextualSkill(gk,['goalkeeping','reflexes','positioning','oneOnOne']):55;const onTarget=clamp(.29+(fin-60)/170+q.xg*.45-q.pressure*.06,.2,.76),onFrame=rng.chance(onTarget),goalP=clamp(q.xg*(.86+(fin-keep)/300),.004,.58),goal=onFrame&&rng.chance(clamp(goalP/onTarget,.02,.78));const localTarget={x:1,y:clamp(.5+rng.range(-.11,.11),.37,.63)},to=team.direction===-1?{x:0,y:1-localTarget.y}:{x:1,y:localTarget.y};return {kind:'shot',shooterId:shooter.id,goalkeeperId:gk?.id||null,xG:q.xg,onTarget:onFrame,goal,to,duration:.32+rng.range(0,.18),height:rng.range(.015,.08),pressure:q.pressure}}
