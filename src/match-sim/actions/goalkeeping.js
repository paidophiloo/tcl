import { contextualSkill } from '../core/player-attributes.js'; import { localPoint, worldPoint } from '../spatial/pitch-model.js';
export function goalkeeperAnchor(team,gk,ball){const b=localPoint(team,ball),sweep=(gk.instructionRole==='sweeperKeeper'||gk.instructionRole==='ballPlayingGoalkeeper');const x=Math.max(.035,Math.min(sweep?.19:.125,.045+b.x*(sweep?.12:.06)));const y=Math.max(.34,Math.min(.66,.5+(b.y-.5)*.28));return worldPoint(team,{x,y})}
export function goalkeeperSkill(gk,context='save'){const map={save:['goalkeeping','reflexes','handling','positioning'],oneOnOne:['oneOnOne','reflexes','composure','positioning'],cross:['commandArea','aerial','handling','bravery'],sweep:['sweeping','anticipation','pace','decisions'],distribution:['kicking','passing','vision','decisions']};return contextualSkill(gk,map[context]||map.save)}
