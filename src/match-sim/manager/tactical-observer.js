import { tacticalMetrics, possessionPercent } from '../analytics/match-analytics.js';
export function observeMatch(state,teamIndex){const metrics=tacticalMetrics(state),team=state.teams[teamIndex],opp=state.teams[1-teamIndex],mine=metrics[teamIndex],theirs=metrics[1-teamIndex];const evidence=[];if(theirs.fieldTilt-mine.fieldTilt>.16)evidence.push({type:'territory',severity:'high',message:'O adversário está sustentando mais posse no terço final.',value:theirs.fieldTilt-mine.fieldTilt});if(opp.stats.xG-team.stats.xG>.65)evidence.push({type:'xg',severity:'high',message:'O adversário está criando chances de melhor qualidade.',value:opp.stats.xG-team.stats.xG});const tired=team.players.filter(p=>!p.redCard).sort((a,b)=>a.stamina-b.stamina)[0];if(tired&&tired.stamina<58)evidence.push({type:'fatigue',severity:'medium',playerId:tired.id,message:`${tired.player.name} perdeu intensidade física.`,value:tired.stamina});const carded=team.players.filter(p=>p.yellowCards>0&&!p.redCard);if(carded.length)evidence.push({type:'cards',severity:'low',playerIds:carded.map(p=>p.id),message:'Há jogadores amarelados em duelos defensivos.'});return {teamIndex,minute:state.clockSeconds/60,scoreDelta:state.score[teamIndex]-state.score[1-teamIndex],possession:possessionPercent(state,teamIndex),metrics:mine,opponentMetrics:theirs,evidence}}
